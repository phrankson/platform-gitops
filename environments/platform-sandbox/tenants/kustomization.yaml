# Lists every tenant onboarded to the platform-sandbox environment.
# Onboarding a new tenant = add their folder here, then list it below.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - platform-services
